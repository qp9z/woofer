org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Chaquopy's Python 3.12 has no armeabi-v7a (32-bit ARM) build, but Flutter
# otherwise force-overrides ndk.abiFilters to all ABIs and Chaquopy then fails.
# This opt-out makes Flutter honor the abiFilters in app/build.gradle.kts
# (arm64-v8a, x86_64). See FlutterPlugin.configureAbiWithoutSplits.
disable-abi-filtering=true
