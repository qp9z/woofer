<?xml version="1.0" encoding="utf-8"?>
<!-- Native launch screen (API 21+): brand ground, nothing else. Artwork-free on
     purpose — a still mark here jumped when the animated one took over. The
     equalizer intro lives entirely in Dart (SplashScreen). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/brand_ground" />
</layer-list>
